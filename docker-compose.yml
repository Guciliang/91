services:
  video-site-91:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: video-site-91
    ports:
      - "9191:9191"
    volumes:
      - ./data:/opt/video-site-91/data
    restart: unless-stopped
